<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen
    xmlns:android="http://schemas.android.com/apk/res/android">

    <tabs>
        <tab
            id="@+id/tab_calls"
            icon="@drawable/logo_upiita"
            title="Calls"
            activeColor="@color/colorAccent"
            />
        <tab
            id="@+id/tab_groups"
            icon="@drawable/logo_upiita"
            title="Groups"
            activeColor="@color/colorAccent" />
        <tab
            id="@+id/tab_chats"
            icon="@drawable/logo_upiita"
            title="Chats"
            activeColor="@color/colorAccent"/>
    </tabs>
</PreferenceScreen>